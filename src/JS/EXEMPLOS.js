/* 1. Declare uma variável chamada `nome` e atribua a ela o seu nome. */

/* 2. Declare duas variáveis, `a` e `b`, e atribua a elas os valores 5 e 10, respectivamente. Em seguida, crie uma variável chamada `soma` e armazene nela o resultado da soma de `a` e `b`. */

/* 3. Crie uma variável chamada `resultado` e armazene nela o resultado da multiplicação de 7 por 8. */

/* 4. Declare uma variável chamada `frase` e atribua a ela a frase: "JavaScript é divertido!". Use a função `console.log` para exibir o valor da variável `frase` no console. */

/* 5. Crie uma variável chamada `idade` e atribua a ela a sua idade. Em seguida, crie uma variável chamada `maiorDeIdade` que armazena o valor booleano indicando se a idade é maior ou igual a 18. */

/* 6. Declare uma variável chamada `x` e atribua a ela o valor 20. Em seguida, use o operador de incremento para aumentar o valor de `x` em 1. */

/* 7. Crie uma variável chamada `divisao` e armazene nela o resultado da divisão de 50 por 5. */

/* 8. Declare uma variável chamada `remainder` e armazene nela o resto da divisão de 15 por 4. */

/* 9. Crie uma variável chamada `texto` e atribua a ela a string "Aprender". Em seguida, utilize o operador de concatenação para unir essa string com a string " JavaScript" e armazene o resultado em uma nova variável chamada `mensagem`. */

/* 10. Declare duas variáveis, `altura` e `largura`, e atribua a elas os valores 1.75 e 0.5, respectivamente. Crie uma variável chamada `area` e armazene nela o resultado da multiplicação de `altura` e `largura`. */

/* 11. Crie uma variável chamada `nota` e atribua a ela um valor numérico entre 0 e 100. Em seguida, use um operador ternário para criar uma variável chamada `status` que armazena "Aprovado" se a nota for
